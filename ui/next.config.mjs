/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      'cdn.pixabay.com',
      'www.lorempixel.com',
      'placeimg.com',
      'picsum.photos',
      'example.com'
    ],
  },
};

export default nextConfig;